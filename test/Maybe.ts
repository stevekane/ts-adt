import * as test from 'tape'
import { Just, Nothing } from '../src/Maybe'

test('Maybe', t => {
  // const m = new Just(5).map(v => v + 5)
  // const n = fmap((v: number) => v + 5, new Nothing)
  // const m2 = flatMap(new Just(5), (v: number) => new Just(v + 5))
  // const n2 = flatMap(new Nothing, (v: number) => new Just(v + 5))
  // const m3 = unit(5)

  // t.true(m instanceof Just && m.val === 10)
  // t.true(n instanceof Nothing)
  // t.true(m2 instanceof Just)
  // t.true(n2 instanceof Nothing)
  // t.true(m3 instanceof Just && m3.val === 5)
  t.end()
})
